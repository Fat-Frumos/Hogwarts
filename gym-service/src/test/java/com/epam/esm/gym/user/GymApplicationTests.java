package com.epam.esm.gym.user;

import org.junit.jupiter.api.Test;
import org.springframework.boot.test.context.SpringBootTest;

/**
 * This class contains test cases to verify the context loading and general functionality
 * of the User application. It ensures that the application context starts correctly and
 * all necessary beans are properly initialized.
 */
@SpringBootTest(classes = GymApplicationTests.class)
class GymApplicationTests {

    @Test
    void contextLoads() {
    }
}
